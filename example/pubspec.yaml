name: easy_radial_gradient_example
description: "Demonstrates easy_radial_gradient."
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.4
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  easy_radial_gradient:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
